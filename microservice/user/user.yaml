# service1/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: user
  namespace: microservices
spec:
  replicas: 2
  selector:
    matchLabels:
      app: user
  template:
    metadata:
      labels:
        app: user
    spec:
      containers:
        - name: user
          image: 495599739945.dkr.ecr.us-east-1.amazonaws.com/user:v1
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: user-config
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
      imagePullSecrets:
        - name: ecr-registry-secret